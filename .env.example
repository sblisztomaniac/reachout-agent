# Reachout Agent - Environment Variables Template
# Copy this file to .env and fill in your actual values

# ============================================================================
# ZERODB CONFIGURATION
# ============================================================================
# ZeroDB is the primary NoSQL database for all application data
# Get your project credentials from https://zerodb.ai dashboard

# Your ZeroDB project ID (obtained after creating project via API or dashboard)
ZERODB_PROJECT_ID=your_project_id_here

# ZeroDB API key (for authentication to ZeroDB endpoints)
ZERODB_API_KEY=your_zerodb_api_key_here

# ZeroDB API base URL (usually this default, but can be overridden)
ZERODB_BASE_URL=https://api.zerodb.ai/v1/public

# ============================================================================
# APPLICATION SECURITY
# ============================================================================

# JWT secret key for signing authentication tokens
# Generate with: python -c "import secrets; print(secrets.token_urlsafe(32))"
JWT_SECRET=generate_a_secure_random_string_here

# JWT token expiration time (in minutes)
JWT_EXPIRATION_MINUTES=1440

# API key for securing backend endpoints (optional for MVP)
API_KEY=your_backend_api_key_here

# ============================================================================
# AI-NATIVE STUDIO APIs
# ============================================================================
# These APIs power agent orchestration, embeddings, and agent bridge

# AI-Native API key (obtain from AI-Native Studio dashboard)
AI_NATIVE_API_KEY=your_ai_native_api_key_here

# AI-Native API base URL
AI_NATIVE_BASE_URL=https://api.ai-native.io/v1/public

# Agent orchestration timeout (seconds) - how long to wait for agent responses
AGENT_ORCHESTRATION_TIMEOUT=300

# Agent Bridge API key (for email/message sending)
AGENT_BRIDGE_API_KEY=your_agent_bridge_key_here

# ============================================================================
# EMBEDDINGS CONFIGURATION
# ============================================================================

# Embedding model to use (default: BAAI/bge-small-en-v1.5)
# IMPORTANT: Must be 384-dimensional model to match datamodel.md
EMBEDDING_MODEL=BAAI/bge-small-en-v1.5

# Embedding similarity threshold (0.0-1.0) for semantic search
EMBEDDING_SIMILARITY_THRESHOLD=0.7

# ============================================================================
# EXTERNAL TOOLS & MCPs
# ============================================================================

# Firecrawl API key (optional - for web scraping in research agent)
# Get from https://firecrawl.dev
FIRECRAWL_API_KEY=optional_firecrawl_key

# Web Search MCP configuration
# Option 1: Local MCP server URL
WEB_SEARCH_MCP_URL=mcp://localhost:5000

# Option 2: Brave Search API (alternative to MCP)
# BRAVE_SEARCH_API_KEY=optional_brave_api_key

# Option 3: Google Search API (alternative)
# GOOGLE_SEARCH_API_KEY=optional_google_key
# GOOGLE_SEARCH_ENGINE_ID=optional_engine_id

# ============================================================================
# EMAIL PROVIDER (for Agent Bridge)
# ============================================================================
# Choose ONE email provider for sending outreach emails

# Option 1: SendGrid
# SENDGRID_API_KEY=your_sendgrid_api_key
# SENDGRID_FROM_EMAIL=noreply@yourdomain.com
# SENDGRID_FROM_NAME=Your Company

# Option 2: Resend
# RESEND_API_KEY=your_resend_api_key
# RESEND_FROM_EMAIL=noreply@yourdomain.com

# Option 3: SMTP (generic email server)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USERNAME=your_email@gmail.com
# SMTP_PASSWORD=your_app_password
# SMTP_FROM_EMAIL=your_email@gmail.com

# ============================================================================
# ENVIRONMENT & LOGGING
# ============================================================================

# Environment mode: development, staging, or production
ENVIRONMENT=development

# Log level: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO

# Enable SQL query logging (for debugging ZeroDB calls)
LOG_SQL_QUERIES=false

# ============================================================================
# FASTAPI CONFIGURATION
# ============================================================================

# FastAPI app host (usually localhost for development)
HOST=0.0.0.0

# FastAPI app port
PORT=8000

# Enable FastAPI debug mode (auto-reload on code changes)
DEBUG=true

# Enable CORS (for frontend development)
CORS_ENABLED=true
CORS_ORIGINS=http://localhost:3000,http://localhost:5173

# ============================================================================
# RATE LIMITING (OPTIONAL)
# ============================================================================

# ZeroDB API rate limit (requests per second)
ZERODB_RATE_LIMIT=10

# Agent orchestration rate limit (concurrent agents)
AGENT_ORCHESTRATION_MAX_CONCURRENT=5

# External API rate limit (requests per minute)
EXTERNAL_API_RATE_LIMIT=60

# ============================================================================
# TESTING & DEVELOPMENT
# ============================================================================

# Use mock ZeroDB client for testing (true/false)
USE_MOCK_ZERODB=false

# Use mock agent orchestration for testing (true/false)
USE_MOCK_AGENTS=false

# Pytest configuration
PYTEST_DISABLE_WARNINGS=true
PYTEST_VERBOSE=true

# ============================================================================
# FUTURE / OPTIONAL SERVICES
# ============================================================================

# LinkedIn API (not available in MVP)
# LINKEDIN_CLIENT_ID=your_linkedin_client_id
# LINKEDIN_CLIENT_SECRET=your_linkedin_client_secret

# Crunchbase API (for company data enrichment - post-MVP)
# CRUNCHBASE_API_KEY=your_crunchbase_key

# Apollo.io API (for email finding - post-MVP)
# APOLLO_API_KEY=your_apollo_key

# ============================================================================
# NOTES
# ============================================================================
#
# CRITICAL: Do NOT commit your .env file to git. It's already in .gitignore.
#
# Setup Steps:
# 1. Copy this file: cp .env.example .env
# 2. Generate JWT_SECRET: python -c "import secrets; print(secrets.token_urlsafe(32))"
# 3. Obtain ZeroDB credentials (see docs/ZeroDB-INIT.md)
# 4. Obtain AI-Native API key from dashboard
# 5. Choose and configure ONE email provider
# 6. (Optional) Configure external tools (Firecrawl, web search)
#
# For detailed setup instructions, see docs/SETUP.md
# For API authentication details, see docs/API-REFERENCE.md
